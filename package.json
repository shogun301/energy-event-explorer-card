{
  "name": "energy-event-explorer-card",
  "version": "0.1.0",
  "description": "Provider-neutral Home Assistant cards for energy history and event timing",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node --check src/engine.js && node --check src/cards.js && node --check dist/energy-event-explorer-card.js",
    "test": "node --test tests/*.test.js",
    "privacy": "node scripts/privacy-scan.js",
    "validate": "npm run build && npm run test && npm run check && npm run privacy"
  },
  "engines": {
    "node": ">=20"
  }
}
